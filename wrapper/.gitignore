# Wrapper build outputs — don't commit binaries.
dist/

# go-winres output (generated by `make build-win` from winres/winres.json)
*.syso

# Go module cache (always gitignored upstream, but local IDEs may resurrect it)
vendor/
